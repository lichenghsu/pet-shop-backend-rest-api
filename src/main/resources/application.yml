spring:
  application:
    name: pet-shop-backend-rest-api
  profiles:
    active: prod

  jackson:
    date-format: com.fasterxml.jackson.databind.util.ISO8601DateFormat
    time-zone: UTC

server:
  port: ${port:8080}
  compression:
    enabled: true
